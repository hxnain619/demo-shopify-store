<div class="flex flex-col items-center my-8">
  <h1 class="text-3xl font-medium my-4 md:my-6">{{ page.title }}</h1>
  <div class="max-w-6xl mx-auto px-4">{{ page.content }}</div>
</div>

{% schema %}
{
  "name": "Default Page Template"
}
{% endschema %}
